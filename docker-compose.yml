services:
  diveplay:
    build: .
    ports:
      - "40001:80"
    restart: unless-stopped

  cloudflared:
    image: cloudflare/cloudflared:latest
    command: tunnel --url http://diveplay:80
    depends_on:
      - diveplay
    restart: unless-stopped
